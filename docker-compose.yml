version: "3.9" 
services:
  backend:
    container_name: claude-ops-backend
    build: ./backend
    ports:
      - "8000:8000"
  
  frontend:
    container_name: claude-ops-frontend
    build: ./frontend
    ports:
      - "5173:5173"
